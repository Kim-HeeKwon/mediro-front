import { Route } from '@angular/router';
import {SignExperienceComponent} from "./sign-experience.component";

export const authSignExperienceRoutes: Route[] = [
    {
        path     : '',
        component: SignExperienceComponent
    }
];
