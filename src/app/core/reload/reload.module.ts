import {NgModule} from "@angular/core";
import {ReloadService} from "./reload.service";

@NgModule({
    imports  : [
    ],
    providers: [
        ReloadService,
    ]
})
export class ReloadModule
{
}
